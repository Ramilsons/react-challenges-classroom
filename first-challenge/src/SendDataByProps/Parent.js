import Child from './Child';

const Parent = () => {
    return(
        <>
            <Child message={'Vai'}/>
            <Child message={'Corinthians'}/>
        </>
    )
}

export default Parent;
